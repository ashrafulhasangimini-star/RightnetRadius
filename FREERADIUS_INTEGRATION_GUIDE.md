# FreeRADIUS এবং RouterOS ইন্টিগ্রেশন - সম্পূর্ণ গাইড

## 🎯 লক্ষ্য

ISP ISP সিস্টেম FreeRADIUS এর সাথে একীভূত করে একটি প্রোডাকশন-গ্রেড RADIUS সার্ভার তৈরি করা।

---

## 📋 সিস্টেম আর্কিটেকচার

```
┌──────────────────────────────────────────────────────────────┐
│                        INTERNET                              │
│                          ↑                                   │
└──────────────┬───────────┬───────────┬────────────────────────┘
               │           │           │
        ┌──────▼─┐  ┌─────▼──┐  ┌────▼──────┐
        │ WiFi AP│  │  WiFi  │  │  Network  │
        │  (1)   │  │  AP(2) │  │   Switch  │
        └────┬───┘  └────┬───┘  └────┬──────┘
             │           │           │
             └───────────┼───────────┘
                         │
           ┌─────────────▼──────────────┐
           │   RouterOS/MikroTik        │
           │   192.168.1.1              │
           │   (NAS - RADIUS CLIENT)    │
           └─────────────┬──────────────┘
                         │
                   RADIUS Protocol
                   Ports: 1812, 1813
                         │
           ┌─────────────▼──────────────┐
           │    FreeRADIUS Server       │
           │    localhost:1812          │
           │    (আমাদের সার্ভার)         │
           └─────────────┬──────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
    ┌─────▼────────┐          ┌────────▼─────────┐
    │  Laravel API │          │  SQLite Database │
    │  (8000)      │          │  (ইউজার ডেটা)    │
    └──────────────┘          └──────────────────┘
```

---

## 📦 যা যুক্ত করা হয়েছে

### 1. **FreeRadiusController.php**
- RADIUS সার্ভার ম্যানেজমেন্ট
- NAS ক্লায়েন্ট যোগ/সম্পাদনা/মুছা
- ইউজার ম্যানেজমেন্ট
- অথেন্টিকেশন টেস্টিং
- এক্সপোর্ট ফাংশনালিটি (FreeRADIUS কনফিগ)

### 2. **API রাউটস** (`routes/api.php`)
```
GET    /api/freeradius/config              - কনফিগ দেখুন
PUT    /api/freeradius/config              - কনফিগ আপডেট
GET    /api/freeradius/status              - সার্ভার স্ট্যাটাস
GET    /api/freeradius/diagnostics        - ডায়াগনস্টিক্স
GET    /api/freeradius/nas-clients        - NAS তালিকা
POST   /api/freeradius/nas-clients        - নতুন NAS যোগ করুন
PUT    /api/freeradius/nas-clients/{id}   - NAS আপডেট করুন
DELETE /api/freeradius/nas-clients/{id}   - NAS মুছুন
GET    /api/freeradius/users              - RADIUS ইউজার
GET    /api/freeradius/export/users       - ইউজার এক্সপোর্ট
GET    /api/freeradius/export/clients     - clients.conf এক্সপোর্ট
POST   /api/freeradius/test-auth          - অথেন্টিকেশন টেস্ট
GET    /api/freeradius/accounting-logs    - অ্যাকাউন্টিং লগ
```

### 3. **কনফিগারেশন ফাইল** (`config/freeradius.php`)
- FreeRADIUS সার্ভার সেটিংস
- নিরাপত্তা কনফিগ
- অ্যাকাউন্টিং সেটিংস
- পারফরম্যান্স অপটিমাইজেশন

### 4. **ডাটাবেস মাইগ্রেশন**
- `nas_clients` - NAS ডিভাইস টেবিল
- `radius_accounting` - অ্যাকাউন্টিং লগ
- `radius_users` - RADIUS ইউজার ক্রেডেনশিয়াল
- `radius_groups` - ইউজার গ্রুপ
- `radius_config_history` - কনফিগ পরিবর্তন লগ
- `radius_health_checks` - সার্ভার হেলথ মনিটরিং

---

## 🚀 ইনস্টলেশন ধাপসমূহ

### Step 1: FreeRADIUS সার্ভার ইনস্টল করুন

#### Windows এ:
```powershell
# Chocolatey ব্যবহার করে ইনস্টল করুন
choco install freeradius-server -y

# অথবা সরাসরি ডাউনলোড করুন
# https://freeradius.org/download/
```

#### Linux এ:
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install freeradius freeradius-mysql -y

# CentOS/RHEL
sudo yum install freeradius freeradius-mysql -y
```

### Step 2: Laravel মাইগ্রেশন চালান

```bash
cd c:\Users\Rajibul Hasan\Documents\GitHub\RightnetRadius

# মাইগ্রেশন চালান
php artisan migrate

# সিডার চালান (অপশনাল)
php artisan db:seed
```

### Step 3: কনফিগ ফাইল কপি করুন

FreeRADIUS কনফিগ ফাইলগুলো সঠিক জায়গায় কপি করুন:

**Windows:**
```
FREERADIUS_CONFIG.md এ দেখানো ফাইলগুলো
→ C:\Program Files\FreeRADIUS\etc\raddb\
```

**Linux:**
```bash
sudo cp radiusd.conf /etc/freeradius/3.0/
sudo cp clients.conf /etc/freeradius/3.0/
sudo cp users /etc/freeradius/3.0/
sudo chown -R freerad:freerad /etc/freeradius/3.0/
```

### Step 4: FreeRADIUS সার্ভার চালু করুন

**Windows (ডিবাগ মোডে):**
```powershell
cd "C:\Program Files\FreeRADIUS\bin"
.\radiusd.exe -X
```

**Linux:**
```bash
sudo systemctl start freeradius
sudo systemctl status freeradius
sudo tail -f /var/log/freeradius/radius.log
```

### Step 5: RouterOS (MikroTik) কনফিগ করুন

RouterOS এর WebUI এ যান:
```
IP → AAA → Radius
```

নতুন RADIUS সার্ভার যোগ করুন:
- **Address:** আপনার Laravel সার্ভারের IP (যেখানে FreeRADIUS চলছে)
- **Secret:** `testing123` (clients.conf এ ম্যাচ করবে)
- **Service:** `login` এবং `ppp`

```
/ip/service
set telnet disabled=yes
set ftp disabled=yes
set www disabled=yes
set ssh disabled=yes
set api disabled=yes
set winbox disabled=yes
set api-ssl disabled=yes
```

### Step 6: টেস্টিং করুন

```bash
# RADIUS অথেন্টিকেশন টেস্ট
radtest user1 password123 127.0.0.1 0 testing123

# Accounting টেস্ট (যদি থাকে)
radclient -f acct.txt -r 3 127.0.0.1:1813 acct testing123
```

---

## 🔌 RouterOS ইন্টিগ্রেশন

### RouterOS এ RADIUS কনফিগারেশন

```mikrotik
# RADIUS সার্ভার যোগ করুন
/radius
add service=login address=127.0.0.1 secret=testing123 realm=login-radius

# PPP সেবার জন্য RADIUS সক্ষম করুন
/ppp/profile
set default-encryption use-encryption=yes

# ইউজার অথেন্টিকেশন চেক করুন
/system/identity
set name="RightnetRadius-Main"
```

### উদ্ভাবনী PPP প্রোফাইল সেটআপ

```mikrotik
# প্রিমিয়াম প্রোফাইল
/ppp/profile
add name=premium-10mbps rate-limit=10M/10M idle-timeout=0

# স্ট্যান্ডার্ড প্রোফাইল
add name=standard-5mbps rate-limit=5M/5M idle-timeout=0

# বেসিক প্রোফাইল
add name=basic-2mbps rate-limit=2M/2M idle-timeout=0
```

---

## 📊 API ব্যবহার উদাহরণ

### 1. NAS ক্লায়েন্ট যোগ করুন

```bash
curl -X POST http://localhost:8000/api/freeradius/nas-clients \
  -H "Content-Type: application/json" \
  -d '{
    "nas_name": "mikrotik-main",
    "nas_ip": "192.168.1.1",
    "shared_secret": "mikrotik_secret_123456",
    "nas_type": "mikrotik",
    "description": "Main RouterOS Gateway"
  }'
```

### 2. অথেন্টিকেশন টেস্ট করুন

```bash
curl -X POST http://localhost:8000/api/freeradius/test-auth \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "password123",
    "nas_ip": "192.168.1.1"
  }'
```

### 3. ইউজার এক্সপোর্ট করুন (FreeRADIUS ফর্ম্যাটে)

```bash
curl -X GET http://localhost:8000/api/freeradius/export/users \
  -o users.txt
```

### 4. clients.conf এক্সপোর্ট করুন

```bash
curl -X GET http://localhost:8000/api/freeradius/export/clients \
  -o clients.conf
```

### 5. সার্ভার স্ট্যাটাস চেক করুন

```bash
curl http://localhost:8000/api/freeradius/status
```

---

## 🛡️ নিরাপত্তা চেকলিস্ট

- [ ] Shared Secret শক্তিশালী করুন (32+ অক্ষর)
- [ ] প্রতিটি NAS এর জন্য ভিন্ন সিক্রেট ব্যবহার করুন
- [ ] Firewall সেটআপ করুন (শুধুমাত্র প্রয়োজনীয় পোর্ট খুলুন)
- [ ] RADIUS লগ নিয়মিত পরীক্ষা করুন
- [ ] ইউজার অডিট চালান
- [ ] ব্যাকআপ সিস্টেম সেটআপ করুন
- [ ] SSL/TLS এনক্রিপশন (RADSec) চিন্তা করুন

---

## 📈 মনিটরিং এবং রক্ষণাবেক্ষণ

### লগ দেখুন

```bash
# Windows
type "C:\Program Files\FreeRADIUS\var\log\radius\radius.log"

# Linux
sudo tail -f /var/log/freeradius/radius.log
```

### ডাটাবেস স্ট্যাটিস্টিক্স

```bash
# সক্রিয় সেশন
curl http://localhost:8000/api/sessions

# অ্যাকাউন্টিং লগ
curl http://localhost:8000/api/freeradius/accounting-logs

# ডায়াগনস্টিক্স
curl http://localhost:8000/api/freeradius/diagnostics
```

---

## 🔧 সমস্যা সমাধান

### সমস্যা: অথেন্টিকেশন ব্যর্থ হচ্ছে

**সম্ভাব্য কারণ:**
1. Shared Secret ম্যাচ না করা
2. ইউজার ডাটাবেসে নেই
3. RADIUS সার্ভার বন্ধ

**সমাধান:**
```bash
# RADIUS সার্ভার চেক করুন
netstat -an | grep 1812

# ইউজার নিশ্চিত করুন
SELECT * FROM users WHERE username = 'user1';

# লগ দেখুন
tail -f /var/log/freeradius/radius.log
```

### সমস্যা: RouterOS সংযোগ ব্যর্থ

**সমাধান:**
1. RouterOS ফায়ারওয়াল চেক করুন
2. RADIUS পোর্ট (1812, 1813) খোলা আছে কিনা দেখুন
3. NAS IP address সঠিক আছে কিনা যাচাই করুন

---

## 📚 অতিরিক্ত সংস্থান

- [FreeRADIUS অফিসিয়াল ডকুমেন্টেশন](https://wiki.freeradius.org)
- [RFC 2865 - RADIUS](https://tools.ietf.org/html/rfc2865)
- [RouterOS RADIUS সেটআপ](https://wiki.mikrotik.com/wiki/Manual:IP/Services/RADIUS)

---

## ✅ চেকলিস্ট

- [ ] FreeRADIUS সার্ভার ইনস্টল করা হয়েছে
- [ ] Laravel মাইগ্রেশন চলেছে
- [ ] কনফিগ ফাইল কপি করা হয়েছে
- [ ] FreeRADIUS সার্ভার চালু হয়েছে
- [ ] RouterOS RADIUS কনফিগ করা হয়েছে
- [ ] অথেন্টিকেশন টেস্ট সফল হয়েছে
- [ ] অ্যাকাউন্টিং লগ কাজ করছে
- [ ] আপনার ইউজাররা সফলভাবে সংযুক্ত হতে পারছে
